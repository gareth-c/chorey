# Dependencies
node_modules/

# Build output
dist/

# Local data (the sqlite db and its WAL/journal files)
data/*.db
data/*.db-*

# Secrets
.env
.env.*
!.env.example

# OS/editor cruft
.DS_Store

# Personal Claude Code settings — .claude/settings.json (if ever added) is
# fine to share, but settings.local.json is per-machine.
.claude/settings.local.json
